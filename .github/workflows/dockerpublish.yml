name: Node.js CI

on:
  push:

  pull_request:


jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [10.x, 12.x]

    steps:
    
    - uses: actions/checkout@v2
##    - name: stop all afrer few minutes
##      run: docker-compose up --abort-on-container-exit & sleep 300 && docker-compose down
       
##    - uses: actions/setup-node@v1
      
      with:
        node-version: ${{ matrix.node-version }}       
    - run: docker-compose up -d && npm --prefix ./e2e i && npm --prefix ./e2e run build && npm --prefix ./e2e run cypress:run && sleep 300 && docker-compose down
##    - run: 
##    - run: 

      env:
        CI: true
